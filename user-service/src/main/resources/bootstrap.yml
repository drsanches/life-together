spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8081

  #TODO: FIX
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    ru.drsanches.user_service: INFO