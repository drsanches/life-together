spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8081

logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    ru.drsanches.auth_service: INFO