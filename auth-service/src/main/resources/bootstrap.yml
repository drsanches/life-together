spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config-service:8081
      fail-fast: true

logging:
  level:
    root: ERROR
    org.springframework.web: ERROR
    ru.drsanches.auth_service: INFO